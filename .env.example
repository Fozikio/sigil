# Sigil Server (ntfy)
SIGIL_TOKEN=your-sigil-auth-token

# Cortex connection
CORTEX_API_URL=https://idapixl-cortex-inkobdxc3q-uc.a.run.app
CORTEX_API_TOKEN=your-cortex-token

# Webhook listener
WEBHOOK_SECRET=your-webhook-secret
WEBHOOK_PORT=3847
DISCORD_BOT_TOKEN=your-discord-token

# GCP (for Firestore access in webhook listener)
# On VPS: mount the service account JSON via Docker secret or bind mount
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp-sa.json
